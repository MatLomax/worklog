# Claude Code SessionStart hook (Windows/PowerShell): warm-load worklog's open
# work into the new session's context. Prints nothing when the project has no
# worklog database yet. worklog.exe must be on PATH.
worklog context
