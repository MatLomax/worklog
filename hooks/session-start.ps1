# Claude Code SessionStart hook (Windows/PowerShell): warm-load worklog's open
# work into the model's context AND show the next task to the user as a visible
# line, in one payload. Prints nothing when the project has no worklog database
# yet. worklog.exe must be on PATH.
worklog session-start
